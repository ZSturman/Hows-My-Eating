type ToggleSidebarProps = {
    isOpen: boolean;
    onToggle: () => void;
    }

const ToggleSidebar: React.FC<ToggleSidebarProps> = ({ isOpen, onToggle }) => {
    return (
       
    );
}

export default ToggleSidebar