# Curated Data

This directory is for reviewed labeled sessions that are suitable for training.

```text
curated/
  accepted_sessions/   # Imported/reviewed sessions used by training
  rejected_sessions/   # Sessions kept for notes, but not used by training
```

Session contents are git-ignored because they may contain personal motion data. Keep the folder structure and documentation tracked.
