# Data Manifests

This directory stores small JSON manifests used to keep training reproducible.

```text
manifests/
  datasets/  # dataset hashes, imported field-test summaries
  splits/    # locked session split manifests
```

Manifests for private/user data may remain local. Public/sample manifests can be committed.
